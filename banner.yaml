banner_submenu:
  view:
    label: Info
    url:
      0: 'admin/view'
      id: $_GET*id
    select: action
  update:
    label: Update
    url:
      0: 'admin/update'
      id: $_GET*id
    select: action
  views:
    label: View(s)
    url:
      0: 'o/view/manage'
      banner: $_GET*id
    select: controller
  clicks:
    label: Click(s)
    url:
      0: 'o/click/manage'
      banner: $_GET*id
    select: controller
  delete:
    label: Delete
    url:
      0: 'admin/delete'
      id: $_GET*id
    htmlOptions:
      data-confirm: Are you sure you want to delete this item?
      data-method: post
    select: action
setting_submenu:
  update:
    label: Settings
    url: ['setting/admin/update']
    select: action
  category:
    label: Categories
    url: ['setting/category']
    select: controller