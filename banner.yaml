banner_submenu:
  view:
    label: Detail
    url:
      0: 'admin/view'
      id: $_GET*id
    select: action
  update:
    label: Update
    url:
      0: 'admin/update'
      id: $_GET*id
    select: action
  views:
    label: View(s)
    url:
      0: 'o/view/manage'
      banner: $_GET*id
    select: controller
  clicks:
    label: Click(s)
    url:
      0: 'rotator/item/manage'
      banner: $_GET*id
    select: controller
  delete:
    label: Delete
    url:
      0: 'admin/delete'
      id: $_GET*id
    htmlOptions:
      data-confirm: Are you sure you want to delete this item?
      data-method: post
    select: action

setting_submenu:
  update:
    label: Settings
    url: ['setting/admin/update']
    select: action
  category:
    label: Categories
    url: ['setting/category']
    select: controller

rotator_submenu:
  0:
    view:
      label: Detail
      url:
        0: 'rotator/admin/view'
        id: $_GET*id
      select: action
    update:
      label: Update
      url:
        0: 'rotator/admin/update'
        id: $_GET*id
      select: action
  1:
    published:
      label: Published(s)
      url:
        0: 'rotator/item/manage'
        category: $_GET*id
        expired: publish
      select: url
    permanent:
      label: Permanent(s)
      url:
        0: 'rotator/item/manage'
        category: $_GET*id
        expired: permanent
      select: url
    pending:
      label: Pending(s)
      url:
        0: 'rotator/item/manage'
        category: $_GET*id
        expired: pending
      select: url
    expired:
      label: Expired(s)
      url:
        0: 'rotator/item/manage'
        category: $_GET*id
        expired: expired
      select: url
    unpublish:
      label: Unpublish(s)
      url:
        0: 'rotator/item/manage'
        category: $_GET*id
        publish: 0
      select: url
    items:
      label: Item(s)
      url:
        0: 'rotator/item/manage'
        category: $_GET*id
      select: url
  2:
    delete:
      label: Delete
      url:
        0: 'rotator/admin/delete'
        id: $_GET*id
      htmlOptions:
        data-confirm: Are you sure you want to delete this item?
        data-method: post
      select: action